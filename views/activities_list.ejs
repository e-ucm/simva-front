<%# views/activities_list.ejs %>
 
<% extend('layout_with_menu_and_sse') %>

<div class="card">
	<h1><%= t('title', { ns : 'activities'}) %></h1>
	<p><%- t('text', { ns : 'activities'}).replace(/\n/g, '</p><p>') %> %></p>
</div>