<%# views/activities_list.ejs %>
 
<% extend('layout_with_menu_and_sse') %>

<div class="card">
	<h1><%= t('activitiesTitle') %></h1>
	<p><%- t('activitiesText').replace(/\n/g, '</p><p>') %> %></p>
</div>