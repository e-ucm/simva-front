<%# views/users_contact_admin.ejs %>

<% extend('layout') %>

<div id="role_selection" class="content">
	<div class="floater">
		<img src="/images/logo_text.png" alt="logo" />
		<h1><%= t("contactadmin.title", { ns : "roles" }) %></h1>
		<p><%- t('welcome.user.text', { username: user.data.username }) %>
		<p><%- t('contactadmin.message', { ns : "roles", email: config.sso.administratorContact }).replace(/\n/g, '<br><br>') %></p>
	</div>
</div>